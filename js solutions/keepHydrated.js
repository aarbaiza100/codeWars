//My OG solution
// function litres(time) {
//     let product = 0.5 * time;
//     while(time > 0) {
//       return Math.floor(product);
//     }
//       return Math.floor(product);
//     }

    
    function litres(time) {
        return Math.floor(time * 0.5);
      }
