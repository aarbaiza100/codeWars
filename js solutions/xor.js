function xor(a,b) {
    
   return a != b;   

}
// p = booleans 
// r = boolean 
// e = 
// false xor false == false // since both are false
// true xor false == true // exactly one of the two expressions are true
// false xor true == true // exactly one of the two expressions are true
// true xor true == false // Both are true.  
// p = 
// function should return true if exactly one of the two expressions evaluate to true. 

//xor only returns true if exaactly one of the expressions evaluate to true 

// 


//if(a || b === true) {
    //return true; 
// } else if (a && b === true){
    //return false; 

//}
